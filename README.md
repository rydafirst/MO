# mobile/ — Rydafirst (React Native / Expo)

Mobile-focused client (customer + rider). Builds against `../contracts/openapi.yaml` and imports
design tokens from `../contracts/design-tokens.ts`. Developed concurrently with web.
